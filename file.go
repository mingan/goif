package main

func NewFileFormatter(orgGroupPrefix, path string) error {
	// open file
	// open temp file
	// format file to temp file
	// if an error occurs, scrap the temp file, return error
	// else replace the file by the temp
	return nil
}
